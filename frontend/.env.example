# Frontend environment variables
# Vite exposes vars prefixed with VITE_ to the browser

# API base URL — leave empty to use Vite's proxy (recommended for dev)
# In production, set to: https://your-backend.com/api
VITE_API_BASE_URL=
